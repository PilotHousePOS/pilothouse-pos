// Type stub for qz-tray — the package ships no TypeScript declarations.
// All QZ Tray calls go through the dynamic-import wrapper in qzTray.ts,
// so `any` here is intentional and correct.
declare module 'qz-tray' {
  const qz: any;
  export default qz;
}
